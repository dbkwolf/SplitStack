package com.example.splitstack.Models

import android.widget.Button

class EventChildItem(expenses: String, participants: String, eventId: String, userId: ArrayList<String>){

    var expenses = expenses
    var participants = participants
    var eventId = eventId
    var userId = userId
}